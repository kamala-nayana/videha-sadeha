name: Build Sadeha PDF Catalogue
on:
  push:
    branches: [main]
    paths-ignore:
      - 'data/sadeha-pdf-catalog.json'
  workflow_dispatch:
permissions:
  contents: write
concurrency:
  group: sadeha-pdf-catalog-main
  cancel-in-progress: true
jobs:
  build-catalog:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Build lightweight Sadeha PDF catalogue
        run: python3 tools/build-sadeha-pdf-catalog.py . data/sadeha-pdf-catalog.json
      - name: Commit catalogue
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add data/sadeha-pdf-catalog.json
          if git diff --cached --quiet; then echo "Sadeha PDF catalogue unchanged."; else git commit -m "Build Sadeha PDF catalogue" && git push; fi
